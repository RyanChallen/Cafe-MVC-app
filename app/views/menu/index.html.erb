<h1>Welcome to Coder Cafe</h1>

<table>
    <tr>
        <th>Item</th>
        <th>Price</th>
    </tr>

    <% @data.each_with_index do |item, index| %>
        <% if item[:quantity] > 0 %>
            <tr>
                <td>
                    <%= link_to "#{item[:item].capitalize}", show_path(index)%>
                    
                    <%#The above is just generating this >>

                    <%# <a href="<%= show_path(index) %><%#">
                        <%#<%= item[:item].capitalize %>
                    <%#</a> %>

                </td>
                
                <td><%= "$%.2f" % item[:price] %></td>
            </tr>
        <% end %>
    <% end %>

</table>